const getters = {
  getMessage: state=> state.message,
  getShowMessage: state=> state.showMessage,
  getCallNow: state=> state.callNow,
  getIsSerch: state=> state.isSerch,
  getCloseUser: state=> state.closeUser,
}
export default getters